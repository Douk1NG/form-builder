import type { Edge } from "@atlaskit/pragmatic-drag-and-drop-hitbox/dist/types/types";

export const Edges = ['left', 'right', 'top', 'bottom'] as Edge[]
export const [LEFT, RIGHT, TOP, BOTTOM] = Edges;