# Parity Screenshots

Drop your form screenshots here. Name them however you like — the agent will rename them to follow the `form-XXX-reference` convention when analyzing.

## Supported formats
- `.png`, `.jpg`, `.jpeg`, `.webp`
